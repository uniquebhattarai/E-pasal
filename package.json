{
  "name": "e-pasal",
  "private": true,
  "scripts": {
    "start": "concurrently -n frontend,server -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
